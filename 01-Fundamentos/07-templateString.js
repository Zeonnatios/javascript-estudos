const nome = "Matheus";
const sobrenome = "Antonio";

const nomeCompleto = `Olá, ${nome} ${sobrenome}`;

console.log(nomeCompleto);
console.log(`1 + 1 = ${1 + 1}`);

const up = (text) => text.toUpperCase();

console.log(up(nomeCompleto));
